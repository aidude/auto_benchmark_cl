# auto_benchmark_cl

Continual learning benchmark for LLM-based systems.

```bash
conda activate twopac
cp .env.example .env
pip install -e ".[dev]"
bash scripts/run_horse_race.sh
```

See `CLAUDE.md` for architecture and session context.
